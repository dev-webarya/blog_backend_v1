# MongoDB Configuration
# Replace with your actual MongoDB Connection URI
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/blogdb?retryWrites=true&w=majority

# Mail Configuration (Gmail SMTP)
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=SecureAdmin@2026

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Optional: Server Port (if you want to override internal port, though Docker maps it)
# SERVER_PORT=8080
